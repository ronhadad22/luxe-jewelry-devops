{{- if and .Values.auth.enabled .Values.auth.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "luxe-jewelry-app.fullname" . }}-auth
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "luxe-jewelry-app.auth.labels" . | nindent 4 }}
  {{- with .Values.auth.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.auth.ingress.className }}
  {{- if .Values.auth.ingress.tls }}
  tls:
    {{- range .Values.auth.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.auth.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "luxe-jewelry-app.fullname" $ }}-auth
                port:
                  number: {{ $.Values.auth.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
